import 'package:carve_app/widgets/resources_maps.dart';
import 'package:flutter/material.dart';

class mapsResources extends StatelessWidget {
  static const routeName = '/food-bank-map';

  const mapsResources({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: ,
      body: Container(
        child: MapScreen(),
      )
    );
  }
}